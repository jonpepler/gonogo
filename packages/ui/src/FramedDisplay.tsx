// Moved to @ksp-gonogo/ui-kit: the rule that visual content goes in a frame is
// written in ui-kit's README, which is the Uplink-facing reference, and an
// Uplink author cannot import from this package. Re-exported rather than
// duplicated, per the styleguide guard that forbids the same name in both.
export { FramedDisplay, type FramedDisplayProps } from "@ksp-gonogo/ui-kit";
