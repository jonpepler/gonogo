/**
 * `Tabs` lives in `@ksp-gonogo/ui-kit`, which is the published package and so
 * the only place an Uplink can reach it. This file is the app-side alias, the
 * same arrangement as Badge / Button / Form / Icons / Panel / Readout and the
 * rest. Add nothing to it; see `Badge.tsx`'s doc comment for why.
 */
export {
  shouldExpandTabs,
  TABS_PANEL_MIN_WIDTH,
  type TabDescriptor,
  Tabs,
  type TabsProps,
} from "@ksp-gonogo/ui-kit";
