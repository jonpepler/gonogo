/**
 * `SettingsService` moved to `@ksp-gonogo/sitrep-sdk`: two Uplink test files
 * construct one to wrap a widget in a `SettingsProvider`, and it named nothing but
 * `PerfBudget`, which is already the sdk's.
 *
 * Re-exported so this package's importers keep their import site.
 */
export { SettingsService } from "@ksp-gonogo/sitrep-sdk";
