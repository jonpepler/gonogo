/** Moved to `@ksp-gonogo/sitrep-sdk/testing`: it is a test helper with no dependencies at all, so there was never a reason an Uplink could not have it. */
export {
  createFakeWallClock,
  type FakeWallClock,
} from "@ksp-gonogo/sitrep-sdk/testing";
